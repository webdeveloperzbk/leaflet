export function createBanner(version) {
	return `/* @preserve
 * Leaflet ${version}, a JS library for interactive maps. https://skpdi.mosreg.ru
 * (c) 2010-${new Date().getFullYear()} СКПДИ2 ЦРЦТ
 */
`;
}
